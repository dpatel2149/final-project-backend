# Infrastructure Layer

This layer contains framework and database-specific code.

Examples in this project:

- Mongoose models
- database connection code
- JWT implementation details

Current runtime files that conceptually belong here:

- `src/models`
- MongoDB connection setup
