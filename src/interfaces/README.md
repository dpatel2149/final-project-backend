# Interfaces Layer

This layer contains entry points into the application.

Examples:

- Express routes
- Express controllers
- HTTP middleware

Current runtime files that conceptually belong here:

- `src/routes`
- `src/controllers`
- `src/middleware`
