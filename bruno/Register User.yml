info:
  name: Register User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/auth/register
  body:
    type: json
    data: |
      {
        "name": "Jeet Test",
        "email": "jeettest1@example.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
