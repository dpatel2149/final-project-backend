info:
  name: Create Post
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:5000/api/posts
  body:
    type: json
    data: |
      {
        "title": "Bruno test post",
        "content": "Created from Bruno"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
