info:
  name: Login User
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  body:
    type: json
    data: |
      {
        "email": "jeettest1@example.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
