info:
  name: Create Post For Like
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:5000/api/posts
  body:
    type: json
    data: |
      {
        "title": "Like Test Post",
        "content": "Created for like test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
