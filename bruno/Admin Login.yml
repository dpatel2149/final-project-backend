info:
  name: Admin Login
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  body:
    type: json
    data: |
      {
        "email": "divytest2@example.com",
        "password": "Password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
